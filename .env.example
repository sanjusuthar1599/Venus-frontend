# Local development — copy to .env.local and adjust if needed
VITE_API_URL=http://localhost:5000

# Production (Render): set in dashboard Environment variables
# VITE_API_URL=https://venus-backend-f9lr.onrender.com
